<h2>
function addUp(n) { <br>
  let sum = 0; <br>
  for (let i = 1; i <= n; i++) {<br>
    sum += i;<br>
  }<br>
  return sum;<br>
}<br>

alert( addUp(4));<br>
alert( addUp(13)  );<br>
alert( addUp(600) );<br>
</h2>
